---
interface Props {
  className?: string;
}
const { className = "" } = Astro.props;
---

<header class={`${className} flex justify-center items-center`}>
  <slot />
  <h1 class="font-title px-2 text-primary text-2xl"><slot name="body" /></h1>
</header>
